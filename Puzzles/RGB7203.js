let number = window.prompt("Та дурын тоо оруулна уу:");
let sum = 0;

function logIOI (n1) {
    for (let i = 0; i < n1; i++) {
        sum = sum + console.log("IOI");
    }
    return sum;
}

sum = logIOI(number);